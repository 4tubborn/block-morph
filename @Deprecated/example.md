#

## format

'''json
{
    "root": [
        {
            "type": "block/item",
            "nbt":{},
            "transformation":{"translation":[x,y,z]}
        }
    ]
}
'''

if type=block

block_state:{Name,Properties}

if type=item
item:{id,components,count}
item_display:<enum>

## other

nbt
